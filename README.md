# Pokemon CLI
by jkassis Ⓒ 2023

# Build
```
> bin/build.sh
```

# Run
```
[I] jkassis@Js-MacBook-Air ~/Co/pokemoncli (main)> bin/run.bash -h                                                                                03.09 21:37
Usage of /app/main:
  -fields string
        fields to retrieve (default "name,type,hp,rarity")
  -limit int
        limit results returned (default 10)
  -page int
        page of results to receive (default 1)
  -query string
        query in query syntax (default "(types:fire or types:grass) hp:[90 to *] rarity:Rare")
```

